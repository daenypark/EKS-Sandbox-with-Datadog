apiVersion: v1
kind: Service
metadata:
  name: spring-boot-demo-service
  namespace: default
  labels:
    app: spring-boot-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: spring-boot-demo
